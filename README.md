# reproductions

### Applications

- **nginx-http-ossm** is an Application that deploys 2 HTTP NGINX Servers behind Red Hat Service Mesh (NoTLS).
- **nginx-http-ossm-tls-gateway-termination** is an Application that deploys an HTTP NGINX Servers behind Red Hat Service Mesh with Ingress Gateway TLS Termination.
